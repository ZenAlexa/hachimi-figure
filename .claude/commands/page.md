Create a Next.js page for: $ARGUMENTS

Requirements:
1. Use the App Router with proper file structure
2. Support internationalization (use next-intl patterns from this project)
3. Include proper metadata for SEO
4. Use Server Components by default, Client Components only when needed
5. Follow the existing layout patterns in /app/[locale]
6. Use the existing UI components from /components
7. Make it responsive and support dark mode
8. Add proper loading and error states if needed

Reference:
- Locale routing: /app/[locale]
- Protected routes: /app/[locale]/(protected)
- Basic layout: /app/[locale]/(basic-layout)
- Messages: /i18n/messages/{locale}/
